# ── RETINAL-AI Django Backend — requirements ──────────────────────────────────
#
# Framework migration: FastAPI  →  Django 4.2 + Django REST Framework
# Server migration:    uvicorn  →  Gunicorn + UvicornWorker (ASGI mode)
#
# No ML dependencies here — PyTorch lives in services/model/ only.

# ── Web framework ─────────────────────────────────────────────────────────────
django>=4.2.0,<5.0
djangorestframework>=3.15.0

# ── CORS (replaces FastAPI CORSMiddleware) ────────────────────────────────────
django-cors-headers>=4.3.0

# ── ASGI server: Gunicorn process manager + Uvicorn async worker ─────────────
gunicorn>=22.0.0
uvicorn[standard]>=0.30.0

# ── Async bridge (ships with Django 4.x; pinned for clarity) ─────────────────
asgiref>=3.8.0

# ── Config (pydantic-settings is framework-agnostic; used by config.py) ──────
pydantic-settings>=2.3.0

# ── HTTP client for model-service + Ollama (async, unchanged) ────────────────
httpx[http2]>=0.27.0

# ── Image processing (vlm_service.py downscales images before VLM calls) ─────
Pillow>=10.3.0

# ── Numerics (advisory_service.py) ───────────────────────────────────────────
numpy>=1.26.0

# ── Removed vs FastAPI version ────────────────────────────────────────────────
# fastapi           — replaced by Django + DRF
# python-multipart  — Django handles multipart file uploads natively
# pydantic          — DRF serializers replace Pydantic response models
